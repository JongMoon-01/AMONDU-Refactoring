apiVersion: v1
kind: Service
metadata:
  name: gpt-backend-service
  namespace: amondu
spec:
  selector:
    app: edtech-gpt-backend
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP
