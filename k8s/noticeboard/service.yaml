apiVersion: v1
kind: Service
metadata:
  name: noticeboard-service
  namespace: amondu
spec:
  selector:
    app: edtech-noticeboard
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP
